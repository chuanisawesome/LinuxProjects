cat<<EOF > test.sh               #puts the lines into the test.sh file until it reaches exit (EOF)

#!/bin/bash
echo "This is the best way to echo in a config file, because WYSIWYG"

EOF                             #This is where it exits
